[project]
name = "edgs_splat_init"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jaxtyping>=0.3.2",
    "romatch",
]

[tool.uv.sources]
romatch = { path = "RoMa" }
